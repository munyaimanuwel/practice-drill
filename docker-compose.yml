services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: drill
      POSTGRES_PASSWORD: drill
      POSTGRES_DB: interview_drill
    ports:
      - "5432:5432"
    volumes:
      - drill_pg:/var/lib/postgresql/data

volumes:
  drill_pg:
